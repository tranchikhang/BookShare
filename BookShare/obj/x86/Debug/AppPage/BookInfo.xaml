<Page x:Class="BookShare.AppPage.BookInfo"
	  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	  xmlns:local="using:BookShare.AppPage"
	  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	  xmlns:mycontrol="using:BookShare.Model.Control"
	  mc:Ignorable="d">
	<Page.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="/Resources/PageResource/BookInfo.xaml" />
			</ResourceDictionary.MergedDictionaries>
		</ResourceDictionary>
	</Page.Resources>
	<Grid Background="{StaticResource BackgroundColor}">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<mycontrol:MyNotification x:ConnectionId='1' x:Name="gridNotification" />
		<ProgressBar x:ConnectionId='2' IsIndeterminate="True"
					 Height="30"
					 Grid.Row="0"
					 Name="progressBar" />
		<Grid Grid.Row="1">
			<ScrollViewer x:ConnectionId='3' Name="mainScrollViewer"
						  Visibility="Collapsed"
						  VerticalScrollBarVisibility="Hidden">
				<Grid>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
					</Grid.RowDefinitions>
					<RelativePanel x:ConnectionId='6' Grid.Row="0"
								   Name="relativePanelInfo">
						<Image x:ConnectionId='11' Height="405"
							   Width="270"
							   Name="BookCover"
							   Source="{Binding image, TargetNullValue={x:Null}}"
							   Stretch="Fill" />
						<StackPanel x:ConnectionId='12' Margin="10,0,0,0"
									Height="Auto"
									Width="Auto"
									Name="stackPanelInfo"
									Orientation="Vertical">
							<TextBlock Style="{StaticResource Title}"
									   Text="{Binding title}" />
							<TextBlock Text="Tác giả"
									   FontFamily="Segoe WP Light" />
							<TextBlock Text="{Binding author}"
									   Style="{StaticResource Author}" />
							<TextBlock Text="Năm" />
							<TextBlock Text="{Binding year}"
									   FontSize="22" />
							<Button x:ConnectionId='13' Style="{StaticResource ButtonAddBook}"
									Name="buttonAddBook"
									                      />
							<Button x:ConnectionId='14' Content="Xem thêm thông tin"
									Style="{StaticResource ButtonViewMore}"
									                             />
						</StackPanel>
					</RelativePanel>
					<ListView x:ConnectionId='7' Grid.Row="1"
							  Margin="5,5,5,5"
							  Visibility="Collapsed"
							  ScrollViewer.VerticalScrollBarVisibility="Hidden"
							  Name="listViewLenders">
						<ListView.ItemContainerStyle>
							<Style TargetType="ListViewItem">
								<Setter Property="HorizontalContentAlignment"
										Value="Stretch" />
								<Setter Property="Padding"
										Value="0" />
							</Style>
						</ListView.ItemContainerStyle>
						<ListView.ItemTemplate>
							<DataTemplate>
								<Grid Margin="0,5,0,5">
									<Grid.ColumnDefinitions>
										<ColumnDefinition Width="100" />
										<ColumnDefinition Width="20" />
										<ColumnDefinition Width="*" />
									</Grid.ColumnDefinitions>
									<Image x:ConnectionId='8' Grid.Column="0"
										   Height="100"
										   Stretch="Fill"
										   Tag="{Binding user.id}"
										                             
										   Source="{Binding user.ava}" />
									<StackPanel Orientation="Vertical"
												Grid.Column="2">
										<TextBlock x:ConnectionId='9' Text="{Binding user.account}"
												   FontSize="24"
												   Tag="{Binding user.id}"
												                              />
										<TextBlock Text="{Binding user.fullAddress}"
												   FontSize="24" />
										<Button x:ConnectionId='10' Content="Mượn"
												                   
												Style="{StaticResource ButtonRequest}"
												IsEnabled="{Binding isSendable}"
												Tag="{Binding id}" />
									</StackPanel>
								</Grid>
							</DataTemplate>
						</ListView.ItemTemplate>
					</ListView>
				</Grid>
			</ScrollViewer>
			<ScrollViewer x:ConnectionId='4' Name="scrollViewerDescription" Visibility="Collapsed">
					<StackPanel Orientation="Vertical">
						<TextBlock Text="Thông tin sách:"
								   Style="{StaticResource TextBlockDescriptionTitle}" />
						<TextBlock x:ConnectionId='5' Name="textBlockDes"
								   TextWrapping="Wrap"
								   Style="{StaticResource TextBlockDescription}" />
					</StackPanel>
				</ScrollViewer>
		</Grid>
		<VisualStateManager.VisualStateGroups>
			<VisualStateGroup>
				<VisualState x:ConnectionId='15' x:Name="WideState">
					<VisualState.StateTriggers>
						<AdaptiveTrigger MinWindowWidth="700" />
					</VisualState.StateTriggers>
					<VisualState.Setters>
						<Setter Target="stackPanelInfo.(RelativePanel.RightOf)"
								Value="BookCover" />
						<Setter Target="stackPanelInfo.(RelativePanel.AlignTopWith)"
								Value="BookCover" />
					</VisualState.Setters>
				</VisualState>
				<VisualState x:ConnectionId='16' x:Name="NarrowState">
					<VisualState.StateTriggers>
						<AdaptiveTrigger MinWindowWidth="0" />
					</VisualState.StateTriggers>
					<VisualState.Setters>
						<Setter Target="stackPanelInfo.(RelativePanel.Below)"
								Value="BookCover" />
						<Setter Target="stackPanelInfo.(RelativePanel.AlignLeftWith)"
								Value="BookCover" />
						<Setter Target="stackPanelInfo.(RelativePanel.AlignTopWith)"
								Value="" />
					</VisualState.Setters>
				</VisualState>
			</VisualStateGroup>
		</VisualStateManager.VisualStateGroups>
	</Grid>
</Page>

